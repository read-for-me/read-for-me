# Read-For-Me (MVP) 화면 설계서 (UI/UX Design Specification) v2.0

> **마지막 업데이트:** 2025-12-25
> **버전 히스토리:** v1.0 (초기 설계) → v2.0 (MVP 완료 + Minimal UI Refactoring)

---

## 1. 디자인 철학 변화

### v1.0 → v2.0 주요 변경점

| 항목 | v1.0 (초기 설계) | v2.0 (현재 구현) |
|-----|-----------------|-----------------|
| **레이아웃** | 2-Column Split (좌 5 : 우 7) | 싱글 컬럼 (max-width: 680px) |
| **UI 스타일** | Card 기반 | 타이포그래피 중심 Minimal |
| **색상 테마** | Blue 계열 (Tailwind blue-600) | Anthropic 스타일 크림/코랄 |
| **배경색** | slate-50, gray-100 | oklch(0.98 0.008 80) 크림색 |
| **다크모드** | 미정 | 지원 (토글 버튼) |
| **스트리밍 UX** | 로딩 스피너 | AI Thinking 실시간 표시 |

### 설계 원칙

1. **읽기 최적화:** 콘텐츠 중심 타이포그래피, 방해 요소 최소화
2. **상태 투명성:** AI 추론 과정을 사용자에게 실시간 표시
3. **미니멀리즘:** 카드/박스 대신 구분선과 여백으로 구조화
4. **동적 적응:** URL 입력창 위치가 상태에 따라 변화

---

## 2. 레이아웃 전략 (현재 구현)

### 2.1 Grid System

- **Container Width:** max-width 680px (중앙 정렬)
- **Responsive:** 모바일/데스크톱 동일한 싱글 컬럼
- **Padding:** 좌우 16px (모바일) ~ 24px (데스크톱)

### 2.2 섹션 구분

```
┌─────────────────────────────────────────┐
│              Header (Sticky)             │
│  Logo  │  [URL Input]  │  🌙  │  GitHub │
├─────────────────────────────────────────┤
│                                         │
│          Content Area (680px)           │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │   Source Panel                    │  │
│  │   - 메타 라인 (플랫폼·날짜·글자수)│  │
│  │   - 제목                          │  │
│  │   - 원문 미리보기 (Collapsible)   │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ─────────── section-divider ──────────  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │   ✨ Bullet points                │  │
│  │   - Thinking (Collapsible)        │  │
│  │   • 요약 포인트 1                 │  │
│  │   • 요약 포인트 2                 │  │
│  │   • 요약 포인트 3                 │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ─────────── section-divider ──────────  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │   🎧 Audio reporting              │  │
│  │   - Thinking (Collapsible)        │  │
│  │   - Script (Collapsible)          │  │
│  │   ┌─────────────────────────────┐ │  │
│  │   │ ▶ ═══════════════════ ⬇️   │ │  │
│  │   │   0:00 / 3:20               │ │  │
│  │   └─────────────────────────────┘ │  │
│  └───────────────────────────────────┘  │
│                                         │
└─────────────────────────────────────────┘
```

---

## 3. 컴포넌트 상세 설계 (현재 구현)

### Zone A: Header (GNB)

- **위치:** 최상단 고정 (Sticky)
- **높이:** ~56px
- **배경:** `bg-background/95 backdrop-blur`

```
┌─────────────────────────────────────────────────────────────┐
│  Read-For-Me [Beta]  │  [URL 입력창]  │  🌙/☀️  │  GitHub  │
└─────────────────────────────────────────────────────────────┘
```

| 요소 | 설명 | 상태별 동작 |
|-----|------|------------|
| **Logo** | "Read-For-Me" + Beta 뱃지 | 항상 표시 |
| **URL Input** | 컴팩트 입력창 (rounded-full) | `idle` 시 숨김, 그 외 표시 |
| **Theme Toggle** | 다크/라이트 모드 전환 | 🌙 ↔ ☀️ |
| **GitHub** | 외부 링크 아이콘 | 항상 표시 |

### Zone B: WelcomeInput (초기 화면)

- **표시 조건:** `status === "idle"` 일 때만
- **위치:** 화면 중앙 (flex 기반)
- **애니메이션:** fade-in + slide-in-from-bottom

```
┌─────────────────────────────────────────┐
│                                         │
│       바쁜 당신을 위한 뉴스 리포팅       │
│   URL만 넣으면 요약과 오디오를 생성합니다  │
│                                         │
│   ┌──────────────────────────────┐      │
│   │  https://...             [→] │      │
│   └──────────────────────────────┘      │
│                                         │
│       GeekNews, Medium 등 지원          │
│                                         │
└─────────────────────────────────────────┘
```

### Zone C: SourcePanel (원문 정보)

- **스타일:** 미니멀 텍스트 기반 (카드 없음)
- **구성 요소:**

| 요소 | 스타일 | 예시 |
|-----|-------|------|
| **메타 라인** | `text-sm text-muted-foreground` | GeekNews · 2025.12.25 · 약 3,200자 |
| **제목** | `text-2xl font-bold` | 기사 제목 |
| **원문 미리보기** | Collapsible | 접기/펼치기 + 원문 전체 보기 링크 |

**상태:**
- `isLoading`: Skeleton UI (3줄)
- `error`: 에러 박스 (destructive 스타일)
- `success`: 데이터 렌더링

### Zone D-1: InsightCard (AI 요약)

- **헤더:** `✨ Bullet points` (section-header 스타일)
- **Thinking Collapsible:** AI 추론 과정 표시

```
┌─────────────────────────────────────────┐
│  ✨ Bullet points                       │
│                                         │
│  🔄 AI가 분석 중... ▾                   │  ← Thinking 접기
│  ┌─────────────────────────────────┐    │
│  │ **핵심 파악**                    │    │
│  │ 글의 주요 논점을 분석 중...      │    │
│  └─────────────────────────────────┘    │
│                                         │
│  [주제 한 줄]                           │
│                                         │
│  • 첫 번째 요약 포인트                  │
│  • 두 번째 요약 포인트                  │
│  • 세 번째 요약 포인트                  │
│                                         │
│  gemini-2.5-flash-preview · 5.2초       │
└─────────────────────────────────────────┘
```

**상태별 UI:**

| 상태 | UI |
|-----|-----|
| `crawling` | Skeleton (3줄) |
| `processing` (streaming) | Thinking shimmer + 실시간 bullet 파싱 |
| `done` | 완성된 요약 + 메타 정보 |
| `error` | 에러 박스 |

**Shimmer 효과:**
```css
.shimmer-text {
  background: linear-gradient(90deg, var(--muted-foreground), var(--foreground), var(--muted-foreground));
  background-size: 200% 100%;
  animation: shimmer 3.5s linear infinite;
  -webkit-background-clip: text;
  color: transparent;
}
```

### Zone D-2: AudioPlayerCard (오디오 리포팅)

- **헤더:** `🎧 Audio reporting` (section-header 스타일)
- **3단계 상태 표시:**

```
┌─────────────────────────────────────────┐
│  🎧 Audio reporting                     │
│                                         │
│  🔄 AI가 분석 중... ▾                   │  ← Script Thinking
│                                         │
│  📄 스크립트 제목 ▾                     │  ← Script Collapsible
│  ┌─────────────────────────────────┐    │
│  │ 문단 1 내용...                   │    │
│  │ 문단 2 내용...                   │    │
│  │ ...                              │    │
│  │ 1,024자 · 10개 문단              │    │
│  └─────────────────────────────────┘    │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │  ▶  ════════════════════  ⬇️   │    │  ← Audio Player
│  │      0:00 / 3:20                │    │
│  └─────────────────────────────────┘    │
│                                         │
│         🔊 오디오 준비 완료 · 180.5초   │
└─────────────────────────────────────────┘
```

**상태별 UI:**

| 상태 | UI |
|-----|-----|
| `scripting` | Thinking shimmer + 스크립트 실시간 스트리밍 |
| `done` (script only) | 스크립트 Collapsible + 비활성 플레이어 |
| `synthesizing` | "음성 합성 중..." 로딩 표시 |
| `ready` | 활성 플레이어 (재생/다운로드 가능) |
| `error` | 에러 박스 |

**오디오 플레이어 컨트롤:**

| 요소 | 구현 상태 |
|-----|----------|
| Play/Pause 버튼 | ✅ 구현 |
| Progress Bar (Slider) | ✅ 구현 |
| Current/Total Time | ✅ 구현 |
| Download 버튼 | ✅ 구현 |
| Rewind/Forward 10s | ❌ 미구현 |
| Speed Control | ❌ 미구현 |

---

## 4. 색상 시스템 (현재 구현)

### 4.1 Light Theme (Anthropic 스타일)

```css
:root {
  --background: oklch(0.98 0.008 80);      /* 크림색 배경 */
  --foreground: oklch(0.25 0.02 50);       /* 다크 브라운 텍스트 */
  --primary: oklch(0.65 0.18 45);          /* 코랄/오렌지 악센트 */
  --muted: oklch(0.94 0.012 75);           /* 연한 베이지 */
  --border: oklch(0.90 0.015 75);          /* 부드러운 구분선 */
}
```

### 4.2 Dark Theme

```css
.dark {
  --background: oklch(0.18 0.015 50);      /* 다크 브라운 배경 */
  --foreground: oklch(0.95 0.01 75);       /* 밝은 크림 텍스트 */
  --primary: oklch(0.70 0.16 45);          /* 밝은 코랄 */
  --muted: oklch(0.28 0.015 50);           /* 어두운 브라운 */
}
```

### 4.3 상태 색상

| 상태 | 색상 | 용도 |
|-----|------|------|
| `primary` | 코랄/오렌지 | CTA 버튼, 강조, bullet marker |
| `destructive` | 레드 | 에러 메시지 |
| `muted-foreground` | 회색 | 보조 텍스트, 메타 정보 |
| `violet-500` | 바이올렛 | Thinking 인디케이터 |
| `green-500` | 그린 | 완료 상태 |

---

## 5. 타이포그래피

### 5.1 폰트 패밀리

```css
@theme inline {
  --font-sans: "Geist", "Geist Fallback";
  --font-mono: "Geist Mono", "Geist Mono Fallback";
}
```

### 5.2 텍스트 스케일

| 용도 | 클래스 | 설명 |
|-----|-------|------|
| **제목** | `text-2xl font-bold` | 기사 제목 |
| **섹션 헤더** | `text-lg font-semibold` | 섹션 타이틀 (아이콘 포함) |
| **본문** | `text-base leading-relaxed` | 읽기 최적화 (typography-reading) |
| **메타** | `text-sm text-muted-foreground` | 메타 정보, 보조 텍스트 |
| **캡션** | `text-xs text-muted-foreground` | 처리 시간, 모델명 |

---

## 6. 애니메이션 & 인터랙션

### 6.1 정의된 애니메이션

| 이름 | 용도 | 설명 |
|-----|------|------|
| `fade-in` | 컴포넌트 등장 | 부드러운 페이드 인 |
| `slide-in-from-bottom-2` | 요소 등장 | 아래에서 슬라이드 |
| `shimmer` | 스트리밍 중 텍스트 | 그라데이션 흐름 효과 |
| `animate-spin` | 로딩 | 스피너 회전 |
| `animate-pulse` | 대기 중 | 깜빡임 효과 |

### 6.2 Collapsible 인터랙션

- **트리거:** 클릭 시 열기/닫기
- **아이콘:** ChevronDown ↔ ChevronUp
- **애니메이션:** 높이 전환 (smooth)

---

## 7. 반응형 동작 (현재 구현)

| 구분 | Desktop | Mobile |
|-----|---------|--------|
| **Container** | max-width 680px, 중앙 정렬 | 동일 |
| **Padding** | px-6 (24px) | px-4 (16px) |
| **Header Input** | 표시 | 표시 |
| **Beta 뱃지** | 표시 | `hidden sm:inline-flex` |
| **구조** | 싱글 컬럼 | 싱글 컬럼 (동일) |

---

## 🚀 향후 UI/UX 개선 계획

### Phase 5: 오디오 플레이어 확장

| 항목 | 설명 | 우선순위 |
|-----|------|---------|
| **Speed Control** | 1.0x / 1.25x / 1.5x / 2.0x 토글 | 🔴 High |
| **Rewind/Forward** | -10s / +10s 버튼 | 🟡 Medium |
| **Waveform 시각화** | 오디오 파형 표시 | 🟢 Low |
| **미니 플레이어** | 스크롤 시 하단 고정 미니 플레이어 | 🟢 Low |

### Phase 6: 입력 UX 개선

| 항목 | 설명 | 우선순위 |
|-----|------|---------|
| **URL 검증 피드백** | 유효하지 않은 URL 입력 시 인라인 에러 | 🔴 High |
| **플랫폼 자동 감지** | URL 입력 시 지원 플랫폼 표시 | 🟡 Medium |
| **최근 입력 히스토리** | 이전에 처리한 URL 목록 | 🟢 Low |

### Phase 7: 접근성 개선

| 항목 | 설명 | 우선순위 |
|-----|------|---------|
| **키보드 네비게이션** | Tab 키로 주요 요소 접근 | 🟡 Medium |
| **Screen Reader** | ARIA 레이블 추가 | 🟡 Medium |
| **고대비 모드** | 시각 장애인을 위한 고대비 테마 | 🟢 Low |

### Phase 8: 고급 UI 기능

| 항목 | 설명 | 우선순위 |
|-----|------|---------|
| **다국어 지원** | 한국어 / English 전환 | 🟢 Low |
| **사용자 설정** | TTS 음성, 요약 길이 등 커스터마이징 | 🟢 Low |
| **공유 기능** | 생성된 콘텐츠 링크 공유 | 🟢 Low |

---

## 8. 디자인 토큰 정리

### CSS Custom Properties (globals.css)

```css
/* Layout */
.content-narrow { max-width: 680px; margin: 0 auto; }
.section-divider { border-top: 1px solid var(--border)/50; margin: 2rem 0; }

/* Typography */
.typography-reading { font-size: 1rem; line-height: 1.625; color: var(--foreground)/90; }
.section-header { display: flex; align-items: center; gap: 0.5rem; font-size: 1.125rem; font-weight: 600; }

/* Components */
.minimal-trigger { display: flex; align-items: center; gap: 0.5rem; width: 100%; }
.bullet-list { display: flex; flex-direction: column; gap: 0.75rem; }
.bullet-marker { width: 6px; height: 6px; border-radius: 50%; background: var(--primary); }
.audio-player-minimal { display: flex; align-items: center; gap: 1rem; padding: 1rem 0; }
```

---

## 9. 버전 히스토리

| 버전 | 날짜 | 주요 변경 |
|-----|------|----------|
| v1.0 | 2025-12-15 | 초기 설계 (2-Column, Card UI) |
| v2.0 | 2025-12-25 | Minimal UI Refactoring, Anthropic 테마, 스트리밍 UX |

---

## 10. 관련 문서

- [PRD.md](./PRD.md) - 프로젝트 요구사항 정의
- [DONE.md](./DONE.md) - 개발 완료 이력 인덱스
- [ADR-015: Minimal UI Refactoring](./done/DONE-20251224.md#adr-015-minimal-ui-refactoring) - UI 리팩토링 결정 기록
- [ADR-017: Anthropic 스타일 색상 테마](./done/DONE-20251224.md#adr-017-anthropic-스타일-색상-테마) - 색상 테마 결정 기록
