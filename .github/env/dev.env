# ==============================================================================
# Read-For-Me Development Environment
# ==============================================================================
# 민감한 정보(API 키 등)는 여기에 넣지 마세요!
# → GCP Secret Manager 사용

# ------------------------------------------------------------------------------
# 기본 설정
# ------------------------------------------------------------------------------
PROJECT_NAME=Read-For-Me
API_V1_STR=/api/v1
ENVIRONMENT=development

# ------------------------------------------------------------------------------
# CORS 설정
# ------------------------------------------------------------------------------
# Vercel Preview URL 패턴 + 로컬 개발
BACKEND_CORS_ORIGINS=["https://read-for-me-ten.vercel.app","http://localhost:3000"]

# ------------------------------------------------------------------------------
# Storage 설정 (GCS)
# ------------------------------------------------------------------------------
STORAGE_BACKEND=gcs
GCS_BUCKET_NAME=read-for-me-data-dev
GCS_PROJECT_ID=gen-lang-client-0039052673
GCS_SIGNED_URL_EXPIRY_MINUTES=60

# ------------------------------------------------------------------------------
# GeekNews 크롤링
# ------------------------------------------------------------------------------
GEEKNEWS_CRAWL_ORIGINAL=false

# ------------------------------------------------------------------------------
# Gemini AI (SummaryService)
# ------------------------------------------------------------------------------
GEMINI_THINKING_LEVEL=low
GEMINI_THINKING_BUDGET=2048

# ------------------------------------------------------------------------------
# Audio Script 생성 (AudioService)
# ------------------------------------------------------------------------------
AUDIO_SCRIPT_MODEL=gemini-2.5-flash
AUDIO_SCRIPT_THINKING_LEVEL=low
AUDIO_SCRIPT_THINKING_BUDGET=2048
AUDIO_SCRIPT_INCLUDE_THOUGHTS=false
AUDIO_SCRIPT_TEMPERATURE=0.5

# ------------------------------------------------------------------------------
# OpenAI TTS
# ------------------------------------------------------------------------------
TTS_MODEL=gpt-4o-mini-tts
TTS_VOICE=marin
TTS_SILENCE_PADDING_MS=500
# TTS_INSTRUCTIONS는 코드 기본값 사용 (한글 인코딩 이슈 방지)

# ------------------------------------------------------------------------------
# Phoenix LLMOps (Dev에서는 비활성화)
# ------------------------------------------------------------------------------
PHOENIX_ENABLED=true
PHOENIX_PROJECT_NAME=read-for-me-dev